platform :ios, '13.0' # Minimum iOS version

target 'Sun and Serve Tennis Club' do
  use_frameworks! :linkage => :static  # Use static linking for performance

  # ✅ Only Firebase Auth (Minimal setup)
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'

  target 'Sun and Serve Tennis ClubTests' do
    inherit! :search_paths
  end

  target 'Sun and Serve Tennis ClubUITests' do
  end
end
